Partner marks used in the prototype. Files are stored locally so the section remains stable after deployment.

Primary sources are the brands' official sites or media assets. `rikom.webp`, `bk-giulini.webp`, and `restart.webp` are reference crops from the user-provided printed presentation because matching downloadable brand files were not publicly available during implementation.
